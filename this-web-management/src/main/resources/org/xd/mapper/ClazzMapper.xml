<!--定义Mapper映射文件的约束和基本结构-->
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.xd.mapper.ClazzMapper">
    <select id="list" resultType="org.xd.pojo.Clazz">
        SELECT c.*, e.name AS masterName
        FROM clazz c
        LEFT JOIN emp e ON c.master_id = e.id
        <where>
            <if test="name != null and name != ''">
                c.name LIKE CONCAT('%', #{name},'%')
            </if>
            <if test="begin != null and begin != ''">
                AND c.end_date &gt;= #{begin}
            </if>
            <if test="end != null and end != ''">
                AND c.end_date &lt;= #{end}
            </if>
        </where>
        ORDER BY c.id DESC
    </select>
    
    <!-- 统计各个班级的学生人数 -->
    <select id="countClazzStudentData" resultType="java.util.Map">
        SELECT c.name, COUNT(s.id) AS total
        FROM clazz c
        LEFT JOIN student s ON c.id = s.clazz_id
        GROUP BY c.id, c.name
        ORDER BY c.id
    </select>
</mapper>